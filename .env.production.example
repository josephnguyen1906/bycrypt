# PRODUCTION — on server copy to `.env.production.local` (and/or `.env`) BEFORE `npm run build`
# Next.js bakes NEXT_PUBLIC_* at build time.

NEXT_PUBLIC_API_URL=https://cms.bycrypt.net
#
# Do NOT put localhost here. Do NOT commit real production env files.
