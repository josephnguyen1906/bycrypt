export enum Role {
  ADMIN = "Admin",
  USER = "User"
}
export enum StatusNotication {
  IMG = 'img',
  TITLE = 'title',
}
export enum StatusSupport {
  LINK = 'link',
  PHONE = 'phone',
}
export enum StatusCheckout {
  RECHARGE = 'recharge',
  WITHDRAW_MONENY = 'withdraw_money',
}
